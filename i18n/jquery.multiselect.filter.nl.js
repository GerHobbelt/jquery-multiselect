/* Dutch initialisation for the jQuery UI multiselect plugin. */
/* Written by Hidde-Jan Jongsma (hidde-jan@vergil.nl). */

(function ( $ ) {

$.extend($.ech.multiselectfilter.prototype.options, {
	label: "Zoek:",
	placeholder: "Geef zoektermen op"
});

})( jQuery );
